# Log

## 2026-07-10

- Milestone 3 (verdict and digest) implemented; checkbox left open on one item, see below. Verdict rules and digest semantics in `docs/specs/verdict-digest.md`; SMTP transport in **ADR-0004** (proposed): smtplib behind a `Notifier` seam, env-only credentials excluded from repr, default port 1025 so a fresh config sends only to a local sink (that default is the owner gate), hand-rolled loopback capture server since stdlib smtpd is gone. Schema v2: `verdicts` (denormalized per cycle, including skips with reasons), `digests` (one per local date, written only after a successful send → failed sends retry next cycle), `cycles.digest_status`. Verdicts are judged from the store each cycle, so they keep flowing during upstream outages. New: `make cycle` / `python3 -m skywatch cycle` (recorded in CLAUDE.md verification commands); `PASSES_BASE_URL`/`FORECAST_BASE_URL` testing-only overrides (config spec updated). Makefile mapping rationale: the new `cycle` target adds a command, not a stack decision — documented in CLAUDE.md instead of touching ADR-0001. Verified: `make test` (107 tests) including loopback-SMTP assertions of exactly one send on the clear fixture and zero on the recorded overcast fixture; manual `make cycle` against a local fixture HTTP server + standalone capture sink delivered exactly one digest (goal-format lines confirmed) for the go case, none for the cloudy case. **Pending for checkbox:** the verification names a Mailpit inbox; Mailpit is not installed and installing it needs owner permission — evidence above uses the repo's own local SMTP sink instead. Owner: grant `brew install mailpit` (or run the check yourself) and this closes.

- Milestone 2 (sources) done. Upstreams chosen in **ADR-0002** (proposed): ISS passes from sat.terrestre.ar (keyless, per-pass `visible` flag — the decisive feature; N2YO rejected for requiring an account, G7VRD for radio-only passes), cloud cover from Open-Meteo (keyless). Storage in **ADR-0003** (proposed): single `DB_PATH` SQLite file, WAL, `user_version` migrations in code, passes replaced-from-now on successful fetch (survives outages, no dupes), forecast hours upserted, cycles audited. New modules: `model`, `fetch`, `sources`, `db`, `cycle`; startup now migrates the DB. Spec: `docs/specs/sources.md`; config spec + `.env.example` gained `DB_PATH`; okf-map rewritten per-module so check-stale stays meaningful (broad `skywatch/**→ADR-0001` produced only noise). Real responses recorded 2026-07-10 as `tests/fixtures/`. Verified: `make test` (59 tests, offline) — triggered cycle against mocks writes 8 pass + 48 forecast rows; timeout/HTTP-500/malformed-shape all survived per source; adr-suggest's persistence flag is covered by ADR-0003.
- Milestone 1 (skeleton) done. Python 3.12+ stdlib-only stack proposed in **ADR-0001** (awaiting owner review): http.server, urllib, sqlite3, smtplib, threading, unittest, make. New: `skywatch/` package (config loading, loopback HTTP server with `/healthz`, entrypoint), `tests/` (18 tests), `Makefile` (`test`, `run`), `.env.example`, README stub, Python ignores in `.gitignore`. Spec added: `docs/specs/config.md` (env contract, fail-fast collected errors, exit 2); mapped in `okf-map.yml`. Recorded the lint decision in CLAUDE.md verification commands (compileall syntax pass inside `make test`; no third-party linter per ADR-0001). Verified: `make test` green offline; `make run` served `/healthz` → `{"status": "ok"}` (root 200, unknown path 404); `LATITUDE=banana make run` exited 2 naming the expected format. Note: Mailpit is not installed on this machine — needed no earlier than milestone 3's manual digest check; will flag before that lands.

## 2026-07-09

- Goal defined via the kit's goal interview (owner answers: stack delegated to proposed ADRs; v1 sources are the core ISS-passes × cloud-cover join only; digest channel is email over SMTP). Journal features — observation log, photo uploads/EXIF, user-data import/export — excluded by the owner as non-goals.
- Known future directions, acknowledged by the owner but deliberately outside the v1 goal (candidates for post-goal milestone proposals): meteor-shower and aurora sources; moon phase/darkness computed locally; paste-anything location and object-designation input (city names, map URLs, plus codes, comet designations); local ephemeris math with tolerance-based golden tests; an `.ics` calendar feed and, later, an OAuth calendar push; a Dockerfile and target-repo CI; a config-file schema; a `skywatch tonight` CLI companion.
