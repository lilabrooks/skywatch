# Log

## 2026-07-11

- Hook audit (both agents), clean bill plus one wording fix. Reviewed and exercised `.claude`/`.codex` hooks end to end in a scratch clone: bash -n and shellcheck clean; mirrors byte-identical; docs-sync silent on a clean tree and on agent-config-only changes, blocks (valid JSON) on modified *and* untracked code without docs, delegates correctly to `scripts/okf check-stale` (a `docs/log.md` change legitimately satisfies the rationale rule — verified in the okf source — and a stale mapping produces the okf block JSON); both env-var forms work from a foreign cwd and the no-var/foreign-cwd case degrades to a safe silent no-op. Version hook: silent when versions match and when offline (curl shimmed to fail), emits valid drift JSON on doctored versions, survives a missing `docs/index.md`. No concurrency conflicts possible: both hooks are read-only (git reads, greps, curl), no temp files or locks, and each agent's wiring points only at its own copies. One defect found and fixed: hook messages said "per the CLAUDE.md workflow", which is wrong guidance inside a Codex session — both now say "the repo playbook (CLAUDE.md / AGENTS.md)", mirrors re-synced.

- Agent-config cleanup, both warts fixed. (1) `AGENTS.md` refreshed: the stale install-time Master objective now reflects the met goal; the fictional find-and-replace paths (`.Codex/settings.json`, `Codex.local.md`) replaced with the real `.codex/` inventory; the `@` import section rewritten as explicit session-start reads (Codex doesn't resolve imports); the `.env`-denial guardrail reworded to be honest about Codex having no mechanical enforcement. CLAUDE.md got the same Master objective refresh plus a bullet documenting the `.codex/`/`AGENTS.md` mirror-and-sync convention. (2) `.codex/hooks.json` no longer hardcodes absolute machine paths — commands are now `bash "${CODEX_PROJECT_DIR:-.}/..."`, and both hook scripts' ROOT resolution gained the same fallback chain (`CLAUDE_PROJECT_DIR` → `CODEX_PROJECT_DIR` → cwd), with the `.claude`/`.codex` copies kept byte-identical. Verified: JSON valid, hooks run from repo root without any var and from a foreign cwd with the var set, 154 tests green.

- Owner decision: commit the Codex CLI agent config. `.codex/` (hooks.json + hook scripts byte-identical to the `.claude` ones) and `AGENTS.md` are now tracked. Both copies of `check-docs-sync.sh` gained `.codex/` and `AGENTS.md` in the agent-config exclusion group — the same treatment `.claude/`/`CLAUDE.md` already get — because untracked agent config was re-triggering the Stop hook every turn (its diff-vs-HEAD design means committed rationales re-arm it). No okf-map entries: nothing governs agent config, same as `.claude/`. Known warts, committed as-is per the owner: `AGENTS.md` is a stale install-time snapshot (still says "no application code yet"), and `.codex/hooks.json` hardcodes absolute machine paths.

- Demo ergonomics: `make demo` (scripts/demo_digest.py) now honors `SMTP_PORT` and `SMTP_TO` from the environment, with CLI flags still winning and a clear exit-2 message on a garbage `SMTP_PORT`; the sink host stays hardcoded to 127.0.0.1 so the demo can never send off-machine. README's port-collision note now offers the move-ports alternative (`mailpit --smtp 127.0.0.1:2525` + `SMTP_PORT=2525 make demo`). Verified live through make on port 2525; 3 new tests (env respected, flags beat env, garbage rejected), 154 green. Staleness rationale for scripts/demo_digest.py → ADR-0004: tooling ergonomics only, the transport decision and its gating are unchanged.

- README: clarified AI-tool attribution in the existing provenance section: Skywatch was built primarily with Claude Code, while Codex assisted with CI, dependency automation, and repository metadata. Documentation only; no product behavior or governed contract changed.

- Added GitHub Actions CI for pull requests and `main`: the offline suite runs on Python 3.12, 3.13, and 3.14, while a separate job checks OKF mappings against the event's base commit. The workflow has read-only repository permission. README badges now report CI status, supported Python, standard-library-only dependencies, MIT licensing, Claude Code provenance, OKF 0.1, and the included specs/ADRs. The owner accepted **ADR-0005**, which records the CI topology and official action dependencies; no application behavior or existing governed contract changed.

- Added `LICENSE` with the standard MIT license text for the repo owner. No product behavior or governed contract changed.

- README: added a port-1025 troubleshooting line to the digest section after the owner hit `bind: address already in use` starting Mailpit (cause: a leftover Mailpit from the milestone-3 verification session still held the port). No code or contract change — both documented sinks share 1025 and the note says how to find and stop the holder.

- Robustness fix after a user report that `make run` said latitude/longitude were "not specified" even after `cp .env.example .env`. Could not reproduce on a clean GitHub clone (README steps work: cp creates `.env`, `make run` loads 13 values and serves). Root cause identified as a latent inconsistency: `apply_env_file` used `name not in environ`, so a variable **set to an empty string** in the shell (e.g. `export LATITUDE=`) counted as "present" and blocked the `.env` value, while every parser treats empty as unset — making `cp` look like a no-op. Fixed: the file now fills any variable that is absent *or* blank in the environment (a non-empty env value still wins). Also added a startup hint to `cp .env.example .env` when required config is missing and no env file was found. Config spec updated; 3 new tests (blank-shadow at unit and subprocess level, missing-file hint); 151 tests green. If the user's actual cause was instead a stale checkout, the report/commit points them to pull.

- **Milestone 3 checked off; the goal is met.** The owner installed Mailpit (v1.30.4); the outstanding manual verification ran against it: go fixture via `make demo` → exactly one digest in the Mailpit inbox ("ISS tonight: 3 watchable passes…", From skywatch@localhost); cloudy variant (time-shifted passes + fully overcast forecast over the real SMTP path) → `skipped: no watchable pass in the next 24h`, inbox total unchanged at 1 — asserted via the Mailpit API and confirmed in the UI. With that, every milestone is checked, all five success criteria pass (offline `make test`, `make run`+healthz, fixture cycle semantics, input tolerance, `git check-ignore .env` + complete `.env.example`), and the first-time-user acceptance pass is recorded above. ADRs 0001–0004 remain `status: proposed`, awaiting owner review; candidate next milestones proposed in the session report (owner selects; GOAL.md changes are owner-reserved).

- Milestone 6 (README quickstart) verified and checked off; first-time-user acceptance pass recorded. From a fresh `git clone`: `cp .env.example .env` → `make test` (148 tests, offline) → `make run` (boot cycle completed against the **real** upstreams; `/healthz` ok; page showed the live cycle) → local sink + `make demo` delivered exactly one digest (subject and body inspected in the sink). Wrong/messy inputs exercised on the clone per the goal's examples: `LATITUDE=banana`, a pasted map URL as `LATITUDE`, `QUIET_HOURS=night`, a missing `.env`, an unknown subcommand, and a taken `PORT` — every one fails fast with a message naming the expected format, no tracebacks. The digest was observed in the repo's documented local sink, not Mailpit (not installed; installing needs owner permission) — M6's Verify clause (README steps work as written) is satisfied; **milestone 3's checkbox stays open** because its Verify clause names a Mailpit inbox specifically. Acceptance-pass findings were fixed in-line and committed: `.env` precedence inversion and the taken-port traceback (both logged above). Two zombie servers from the pre-fix acceptance attempt were killed during the pass — an operational reminder that `make run` has no pidfile/stop story (candidate milestone material, noted below).

- Acceptance-pass finding, fixed: `.env` precedence was inverted. The Makefile sourced `.env` over the caller's environment, so `LATITUDE=banana make run` with a valid `.env` started a healthy server instead of failing fast, and `PORT=9000 make run` was silently ignored. Skywatch now loads the env file itself at startup (`SKYWATCH_ENV_FILE` overrides the path; empty string disables — the subprocess tests use that for hermeticity) and only fills variables the environment doesn't already set. Config spec, `.env.example`, README, and ADR-0001's consequence line updated; 9 new tests (parser rules + process-level precedence). `scripts/demo_digest.py` staleness rationale: its change is a cosmetic log-suppression fix (subclass instead of patching a functools.partial), no contract change against ADR-0004.

- Milestone 6 (README quickstart) content landed: full README (quickstart, digest demo, configuration pointers, verdict rules summary, layout) plus `make demo` / `scripts/demo_digest.py` — a deterministic layer-3 digest demo that time-shifts the recorded fixtures so "tonight" always has a clear visible pass and drives the real SMTP path into a local sink (Mailpit or `python3 -m tests.smtp_capture`); needed because the raw recorded fixtures age out of the forward-looking digest window. ADR-0004 layer-3 text updated accordingly; demo script and its two end-to-end tests mapped to ADR-0004. Makefile staleness rationale: the `demo` target is another command addition, not a stack decision. Verification (clean clone) runs after this commit; checkbox follows with it.

- Milestone 5 (scheduler and operations) done. Serve mode now runs a cycle immediately, then every `FETCH_INTERVAL_MINUTES` (default 360) on a daemon thread that survives crashing cycles; scheduler and the new `POST /cycle` trigger share one lock-serialized `CycleRunner` (503 when unwired, 500-no-traceback on crash). `QUIET_HOURS` (local, may cross midnight, start-inclusive/end-exclusive) suppresses only the digest send and does not consume the day's dedup slot. Each cycle prunes rows strictly older than `RETENTION_DAYS` (default 30) behind the injectable clock; verdicts follow their pruned cycle so the FK never trips. `.env.example` now documents every variable in the config spec. Spec: `docs/specs/operations.md`; verdict-digest precedence updated (quiet hours slot); config spec grew the three rows. adr-suggest's scheduling flag: covered by ADR-0001's existing scheduler decision (threading interval loop) — no new decision to record. Fixed in passing: the milestone-4 populated-page HTTP test was coupled to real time through the handler's clock (it started failing once wall time passed the fixture passes); the server now takes an injectable clock like everything else, and the startup boot test got fixture-URL/DB overrides since serve mode fires a real cycle at boot. Verified: `make test` (138 tests — scheduler cadence/crash-survival/stop, retention across all five tables incl. FK edge and at-cutoff boundary, quiet-hours cycle behavior, trigger routes); manual serve-mode smoke against local fixture servers (boot cycle + POST /cycle + live page); `git check-ignore .env` passes.

- Milestone 4 (status page) done. `/` renders the newest cycle's verdicts whose window hasn't ended (soonest first: local window, elevation, compass path, cloud, badge, reason — skips shown with reasons on purpose), a last-cycle line with all three statuses verbatim, and two deliberate empty states (no cycles → `make cycle` hint; no upcoming passes). `/style.css` served from a module constant; render failures return a plain 500 with the traceback only in the log; all dynamic values escaped (test injects `<script>` via a status string); per-request connections under WAL. Spec: `docs/specs/status-page.md`; `db.py` page readers mapped to it. `skywatch/__main__.py` staleness rationale: the change is one-line wiring for the new `make_server(config)` signature; the config contract it maps to is unchanged. Verified: `make test` (115 tests, routes + asset + escaping + 500 path); manual browser check of both states (empty DB and fixture-seeded DB, dark scheme respected) — screenshots reviewed in session.

- Milestone 3 (verdict and digest) implemented; checkbox left open on one item, see below. Verdict rules and digest semantics in `docs/specs/verdict-digest.md`; SMTP transport in **ADR-0004** (proposed): smtplib behind a `Notifier` seam, env-only credentials excluded from repr, default port 1025 so a fresh config sends only to a local sink (that default is the owner gate), hand-rolled loopback capture server since stdlib smtpd is gone. Schema v2: `verdicts` (denormalized per cycle, including skips with reasons), `digests` (one per local date, written only after a successful send → failed sends retry next cycle), `cycles.digest_status`. Verdicts are judged from the store each cycle, so they keep flowing during upstream outages. New: `make cycle` / `python3 -m skywatch cycle` (recorded in CLAUDE.md verification commands); `PASSES_BASE_URL`/`FORECAST_BASE_URL` testing-only overrides (config spec updated). Makefile mapping rationale: the new `cycle` target adds a command, not a stack decision — documented in CLAUDE.md instead of touching ADR-0001. Verified: `make test` (107 tests) including loopback-SMTP assertions of exactly one send on the clear fixture and zero on the recorded overcast fixture; manual `make cycle` against a local fixture HTTP server + standalone capture sink delivered exactly one digest (goal-format lines confirmed) for the go case, none for the cloudy case. **Pending for checkbox:** the verification names a Mailpit inbox; Mailpit is not installed and installing it needs owner permission — evidence above uses the repo's own local SMTP sink instead. Owner: grant `brew install mailpit` (or run the check yourself) and this closes.

- Milestone 2 (sources) done. Upstreams chosen in **ADR-0002** (proposed): ISS passes from sat.terrestre.ar (keyless, per-pass `visible` flag — the decisive feature; N2YO rejected for requiring an account, G7VRD for radio-only passes), cloud cover from Open-Meteo (keyless). Storage in **ADR-0003** (proposed): single `DB_PATH` SQLite file, WAL, `user_version` migrations in code, passes replaced-from-now on successful fetch (survives outages, no dupes), forecast hours upserted, cycles audited. New modules: `model`, `fetch`, `sources`, `db`, `cycle`; startup now migrates the DB. Spec: `docs/specs/sources.md`; config spec + `.env.example` gained `DB_PATH`; okf-map rewritten per-module so check-stale stays meaningful (broad `skywatch/**→ADR-0001` produced only noise). Real responses recorded 2026-07-10 as `tests/fixtures/`. Verified: `make test` (59 tests, offline) — triggered cycle against mocks writes 8 pass + 48 forecast rows; timeout/HTTP-500/malformed-shape all survived per source; adr-suggest's persistence flag is covered by ADR-0003.
- Milestone 1 (skeleton) done. Python 3.12+ stdlib-only stack proposed in **ADR-0001** (awaiting owner review): http.server, urllib, sqlite3, smtplib, threading, unittest, make. New: `skywatch/` package (config loading, loopback HTTP server with `/healthz`, entrypoint), `tests/` (18 tests), `Makefile` (`test`, `run`), `.env.example`, README stub, Python ignores in `.gitignore`. Spec added: `docs/specs/config.md` (env contract, fail-fast collected errors, exit 2); mapped in `okf-map.yml`. Recorded the lint decision in CLAUDE.md verification commands (compileall syntax pass inside `make test`; no third-party linter per ADR-0001). Verified: `make test` green offline; `make run` served `/healthz` → `{"status": "ok"}` (root 200, unknown path 404); `LATITUDE=banana make run` exited 2 naming the expected format. Note: Mailpit is not installed on this machine — needed no earlier than milestone 3's manual digest check; will flag before that lands.

## 2026-07-09

- Goal defined via the kit's goal interview (owner answers: stack delegated to proposed ADRs; v1 sources are the core ISS-passes × cloud-cover join only; digest channel is email over SMTP). Journal features — observation log, photo uploads/EXIF, user-data import/export — excluded by the owner as non-goals.
- Known future directions, acknowledged by the owner but deliberately outside the v1 goal (candidates for post-goal milestone proposals): meteor-shower and aurora sources; moon phase/darkness computed locally; paste-anything location and object-designation input (city names, map URLs, plus codes, comet designations); local ephemeris math with tolerance-based golden tests; an `.ics` calendar feed and, later, an OAuth calendar push; a Dockerfile and target-repo CI; a config-file schema; a `skywatch tonight` CLI companion.
